
<div class="col-lg-3 col-md-4 col-xs-12 ">
    <div class="search-container result-div">
        <div id="rid-button" index="{{index}}">description</div>
        <div id="add-button">add</div>
        <img src="{{imageLink}}" alt="book cover" class="img-responsive img-thumbnail">
        <div class="info-container">
            <h4>{{title}}</h4>
            {{#each authors}}
                <h5>{{this}}</h5>
            {{/each}}
        </div>
    </div>
</div>
